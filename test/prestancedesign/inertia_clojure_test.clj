(ns prestancedesign.inertia-clojure-test
  (:require [clojure.test :refer :all]
            [prestancedesign.inertia-clojure :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
